@model IEnumerable<FootballClubLibrary.Models.Pilkarz>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Pozycja)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Wynagrodzenie)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Klub)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Pozycja)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Wynagrodzenie)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Klub.IdKlub)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdPilkarz">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdPilkarz">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdPilkarz">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
